version: 0.2
phases:
  install:
    commands:
      - echo "Nothing to install for static html "
  build:
    commands:
      - echo "Zipping files"
      - zip -r app.zip index.html appspec.yml buildspec.yml
artifacts:
  files:
    - app.zip